"""Admin endpoints — placeholder for administration and management.

This router will host administrative operations (seed data, cache management,
etc.). Endpoints to be implemented in later slices.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/admin")

# TODO: Implement admin endpoints in subsequent slices.
