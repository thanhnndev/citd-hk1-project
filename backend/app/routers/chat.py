"""Chat endpoints — placeholder for conversation and query handling.

This router will host the primary user-facing API for the assistant.
Endpoints to be implemented in later slices.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/chat")

# TODO: Implement chat endpoints in subsequent slices.
