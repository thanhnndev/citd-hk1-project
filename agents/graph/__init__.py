# Agents — Graph layer (LangGraph StateGraphs)

# Streaming adapter for LangGraph → SSE marker mapping (T04)
try:
    from agents.graph.streaming import StreamingAdapter, stream_graph_to_sse
except Exception:
    pass
